{
  "name": "backend",
  "version": "0.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --rs ./src/index.ts",
    "build": "rimraf .dist && tsc --build ./tsconfig.json",
    "start": "node ./dist/index.js",
    "pretify": "prettier --log-level warn --cache --write '{src/**/*, *}.{ts, tsx, js, json, yml, scss}' '!**/{node.modules, dist}/**/*'"
 
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
